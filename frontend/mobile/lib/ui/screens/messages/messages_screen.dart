import 'package:flutter/material.dart';

class MensajesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Mensajes Screen'),
    );
  }
}
