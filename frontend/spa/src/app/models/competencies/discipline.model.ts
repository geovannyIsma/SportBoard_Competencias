import { RuleDiscipline } from './rule-discipline.model';

export interface Discipline {
    id: number;
    name: string;
    image: string;
    surface: string;
    federation: string;
    description: string;
}